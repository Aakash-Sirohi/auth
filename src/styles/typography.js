import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '32px',
  baseLineHeight: 42,
  paddingtop: 40,
  headerFontFamily: ['Metropolis'],
  bodyFontFamily: ['Metropolis'],
});

export default typography;
